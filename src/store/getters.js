export default {
  // loading: state => state.isLoading,
  // token: state => state.token,
  // me: (state) => {
  //   console.log(state.user)
  //   return state.user
  // },
  // teams: state => state.team.teams,
  // rooms: state => state.room.rooms,
  // contacts: state => state.contact.contacts,
  // contactAccounts: state => state.contact.contacts.map(contact => contact.account),
  // tags: state => state.team.tags,
  // getTeamById: (state) => (id) => {
  //   return state.team.teams.find(team => team._id === id)
  // }
}
